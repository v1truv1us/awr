# Continue: awr-blank-screen-debug-fix

**Previous session**: `~/.claude/plans/i-tried-to-use-purring-wombat.md`
**Session ID**: `737d7f4e-2049-4791-8803-76464096be84`
**Status**: Session hit Claude rate limit mid-investigation

## What was completed

### Step 1 — Diagnosis (DONE)
- Rebuilt binary, ran tests (`test-dom`, `test-page` pass)
- Fetched NVIDIA URL HTML (236 KB), analyzed structure
- **Key findings**:
  - `<main>` is nearly empty (61 bytes, 0 visible text)
  - `<body>` has 2393 bytes of visible text outside templates
  - 14 `<template>` elements found — visible content lives inside them
  - Next.js uses `BAILOUT_TO_CLIENT_SIDE_RENDERING` (deliberately bails to CSR)
  - Root causes confirmed: (1) JS engine can't drive Next.js, (2) sync script fetches block TUI, (3) browse heuristics over-prune app shells, (4) `<template>.content` not exposed in DOM bridge

### Step 2 — Code changes (PARTIALLY DONE)
**Already landed (653 insertions, 7 files modified, tests pass, compiles clean)**:
- `src/browser.zig` (+250) — form handling, editableFields work
- `src/render.zig` (+297) — form rendering improvements  
- `src/dom/bridge.zig` (+61) — new DOM bridge APIs
- `src/tui.zig` (+5) — TUI changes
- `src/page.zig` (+23/-2) — page orchestration
- `src/main.zig` (+12) — CLI changes
- `tests/wpt_runner.zig` (+20) — WPT test additions

### Not yet done from original plan
- **A.** Surface "no terminal-friendly content" status message in TUI (`src/browser.zig` `loadUrl`)
- **B.** Fix `BrowserSession.editableFields` to filter submit fields

### User's new direction (last message before rate limit)
> "preventing awr from displaying javascript heavy pages makes less friendly to what I want AWR to be able to do. It should be able to load all webpages in the terminal TUI..."

The user wants **real fixes** for JS-heavy pages, not just a diagnostic message.

## What remains — Next steps

### 1. Complete original plan items A & B
- Add "no terminal-friendly content" status message in `browser.zig` `loadUrl` (~line 439)
- Fix `editableFields` to properly filter submit-typed fields

### 2. Expose `<template>` content in DOM bridge
The session was investigating this when it was cut off. The NVIDIA page's real content is inside `<template>` elements. The DOM bridge (`src/dom/bridge.zig`) currently has no `template.content` exposure.
- In `src/html/parser.zig` or `src/dom/node.zig`: when parsing `<template>`, extract its children into the DOM tree (or expose them via a `.content` fragment)
- This is the **highest-impact fix** — it would surface SSR content that Next.js puts in templates

### 3. Relax browse heuristics for app shells
- `src/browse_heuristics.zig:74-80` — `shouldSkipForBrowse` drops nodes with `linkDensity >= 0.55` or `text_bytes < 120`
- Card grids have high link density per card, so SSR text gets filtered
- Consider lowering thresholds or adding an escape hatch for `<main>` children

### 4. Verify
- `zig build test-dom test-page test-e2e`
- Manual: `awr browse https://build.nvidia.com/models?filters=nimType%3Anim_type_preview&label=coding`
- Manual: `awr browse https://example.com` — regression check

## Critical files
- `src/browser.zig` — TUI session, `loadUrl` (439), draw loop (567+), `editableFields` (~195)
- `src/render.zig` — `renderBrowseModel` (355), `renderModelFromRoot` (384)
- `src/browse_heuristics.zig` — `chooseContentRoot` (30), `shouldSkipForBrowse` (74)
- `src/dom/bridge.zig` — DOM↔JS bridge (template.content not exposed)
- `src/dom/node.zig` — DOM tree types
- `src/html/parser.zig` — HTML→DOM conversion
- `src/page.zig` — `processHtml` (258-385), `drainAll` (232)
