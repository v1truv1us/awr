# Plan: Document WPT Test Status

## Task
Update `spec/subspecs/wpt-conformance.md` §8 (mapping table) to accurately reflect the current state of curated WPT tests — which cases are active and passing vs. deferred.

## Problem
The existing §8 table lists intended cases that don't exist yet (e.g. `mutation_create_append.js`, `form_method_post.js`, `cookies_persistence_roundtrip.js`) and lists POST tests as active when they're deferred. It needs to match reality.

## File to Modify
- `spec/subspecs/wpt-conformance.md` — update §8 mapping table

## Changes
1. Split §8 into two sections:
   - **§8a: Active curated cases (59 passing)** — every test file actually registered in `wpt_runner.zig`, grouped by area
   - **§8b: Deferred cases** — POST round-trip tests and other cases that exist in `tests/wpt/` but aren't registered, with the blocking reason (e.g. "requires in-process echo server fixture")
2. Add a brief current-status note at the top of §8 confirming all 59 active cases pass via `zig build test-wpt`

## Verification
- `zig build test-wpt` exits 0
- Read the updated spec to confirm it matches the actual `curated_cases` array in `wpt_runner.zig`