# Plan: Formulate MVP Completion Prompt for Claude Code

## Summary

Create a production-ready prompt for Claude Code that guides it through completing all remaining AWR MVP backlog items. The prompt already exists as a draft at `/Users/johnferguson/Github/awr/PROMPT_COMPLETE_MVP.md` (479 lines) and covers:

1. **P0 — Build fix**: Zig 0.16 migration (10 compilation errors), gzip decompression, experiment log refresh
2. **P1 — Test coverage**: WPT curated set 24→~40 assertions, Test262 7→~20 cases
3. **P2 — Verification**: MLKEM768 byte-level assertion, concurrent pool stress test
4. **P3 — Docs**: Collapse phase-named planning docs
5. **Final verification**: Full build + all 6 test gates + binary smoke tests

## Deliverable

The file `PROMPT_COMPLETE_MVP.md` at the repo root is the prompt. It needs minor cleanup:
- Remove the note about "10 errors" since the exact count may vary by the time it's used
- Verify Zig 0.16 API references are accurate (the agent inferred some from context)
- Optionally consolidate into the plan file or keep as a standalone file

## Verification

- The prompt is self-contained and actionable
- Includes exact file paths and line numbers
- Includes verification commands after each task
- Includes invariant rules that must be followed
- Ordered by dependency (build fix first)
