# Changelog

## 0.1.0 - 2026-05-07

- Added a pi package that registers a `web` tool backed by AWR
- Added degraded `visit` fallback when AWR fails
- Added problem logging and helper commands for inspecting AWR failures
- Added unit tests for config, fallback parsing, AWR envelope parsing, and logging
